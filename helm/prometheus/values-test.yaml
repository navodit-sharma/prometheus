---

global:
  ambassador:
    enabled: true
  kube-prometheus-stack:
    enabled: true
  prometheus-elasticsearch-exporter:
    enabled: false
  grafanaAdminPassword: change-me-length-min-12
  externalUrl: "change-me"
  smtpApiKey: change-me
  smtpDestinationAddress: "change-me"
  web:
    ssl:
      enabled: true
      letsencrypt:
        enabled: true
        certManagerApiMigration: true
      externalSecret:
        enabled: false
        keyVaultName: "change-me"

ambassador:
  service:
    loadBalancerIP: "0.0.0.0" # Change me

prometheus-elasticsearch-exporter:
  es:
    uri: 'http://aux-elasticsearch-master.shared.svc.cluster.local:9200'

kube-prometheus-stack:
  alertmanager:
    enabled: true
    alertmanagerSpec:
      externalUrl: "http://change-me/alertmanager"
      storage:
        volumeClaimTemplate:
          spec:
            storageClassName: "managed-premium"
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 5Gi
  grafana:
    persistence:
      size: 10Gi
      storageClassName: "managed-premium"
    env:
      GF_SMTP_ENABLED: "true"
  prometheus:
    prometheusSpec:
      externalLabels:
        cluster: "change-me"
      storageSpec:
        volumeClaimTemplate:
          spec:
            storageClassName: "managed-premium"
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 50Gi
